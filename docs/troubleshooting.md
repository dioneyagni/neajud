# Troubleshooting

## Problemas comuns

### ImageMagick não processa PSD

Instalar:

```bash
sudo apt install imagemagick libmagickwand-dev
```

### SolidQueue não executa

Executar:

```bash
bin/rails solid_queue:restart
```
