<h2><%= molde.new_record? ? "Register New Molde" : "Edit Molde" %></h2>

<%= form_with model: molde, local: true, class: "molde-form" do |f| %>
  <% if molde.errors.any? %>
    <div class="flash flash--error">
      <%= molde.errors.full_messages.join(", ") %>
    </div>
  <% end %>

  <div class="client-field">
    <%= f.label :nome, "Name" %>
    <%= f.text_field :nome, required: true, autofocus: true %>
  </div>

  <div class="molde-componentes-section"
       data-controller="componentes"
       data-componentes-search-url-value="<%= search_pecas_path %>"
       data-componentes-register-url-value="<%= new_peca_path(molde_id: molde.id) %>">
    <h3>Componentes</h3>

    <div class="componentes-buscar">
      <input type="text" class="combobox-input componentes-input"
             placeholder="Buscar peça..."
             data-componentes-target="input"
             data-action="input->componentes#search blur->componentes#blur">
      <div class="combobox-results" data-componentes-target="results"></div>
    </div>

    <ul class="componentes-lista" data-componentes-target="lista">
      <% molde.pecas.each do |peca| %>
        <li data-peca-id="<%= peca.id %>">
          <span class="componente-nome"><%= peca.nome %></span>
          <input type="hidden" name="molde[peca_ids][]" value="<%= peca.id %>">
          <button type="button" class="btn btn-danger btn-sm" data-action="click->componentes#remove">&times;</button>
        </li>
      <% end %>
    </ul>

    <template data-componentes-target="model">
      <li>
        <span class="componente-nome"></span>
        <input type="hidden" name="molde[peca_ids][]" value="">
        <button type="button" class="btn btn-danger btn-sm" data-action="click->componentes#remove">&times;</button>
      </li>
    </template>
  </div>

  <div style="margin-top: 1.5rem; display: flex; gap: 0.5rem;">
    <%= f.submit molde.new_record? ? "Register Molde" : "Update Molde", class: "btn btn-primary" %>
    <%= link_to "Cancel", moldes_path, class: "btn btn-secondary" %>
  </div>
<% end %>

<div class="back-link-wrapper">
  <%= link_to "Back to Moldes", moldes_path, class: "back-link" %>
</div>
